# Photos (not versioned)

Everything in this folder except this file is ignored by git. Photos are added with
`tools/corpus/ingest.py`; only the OCR dumps (`corpus/ocr/`) and the expected values
(`corpus/expected/`) are committed.
